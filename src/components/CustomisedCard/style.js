const styles = (theme) => ({
    root: {
        maxWidth: 345,
        minHeight:400
      },
      media: {
        height: 200,
      },
      text:{
        fontWeight:'bold'
      }
})

export default styles;