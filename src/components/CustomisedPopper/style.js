const styles = (theme) => ({
    paper: {
		border: '1px solid',
		padding: theme.spacing(2,3,2,3),
		backgroundColor: theme.palette.background.paper
	},
	popper: {
        float: 'right',
        zIndex:50
    },
    icon:{
        color:'#fff',
        padding:0
    }
})

export default styles;